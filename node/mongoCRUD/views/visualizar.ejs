<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pilotos F1</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            min-height: 100vh;
            margin: 0;
        }

        .card {
            display: flex;
            width: 400px;
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin: 15px;
            overflow: hidden;
        }

        .card img {
            width: 150px;
            height: 150px;
            object-fit: cover;
            border-right: 1px solid #ddd;
        }

        .card-content {
            padding: 15px;
            flex: 1;
        }

        .card-content h3 {
            margin-top: 0;
            color: #333;
        }

        .card-content p {
            margin: 5px 0;
            color: #555;
        }

        .card-content span {
            font-weight: bold;
            color: #000;
        }
        .buttons {
            margin-top: 15px;
            display: flex;
            justify-content: space-between;
        }
        .buttons a {
            text-decoration: none;
            padding: 10px 20px;
            border-radius: 4px;
            color: white;
            background-color: #007bff;
            transition: background-color 0.3s ease;
        }
        .buttons a:hover {
            background-color: #0056b3;
        }
        .buttons a.delete {
            background-color: #dc3545;
        }
        .buttons a.delete:hover {
            background-color: #c82333;
        }
        .add-button {
            position: fixed;
            bottom: 20px;
            right: 20px;
            padding: 15px 30px;
            background-color: #28a745;
            color: white;
            text-decoration: none;
            border-radius: 50px;
            font-size: 18px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transition: background-color 0.3s ease;
        }
        .add-button:hover {
            background-color: #218838;
        }
    </style>
</head>
<body>
    <% if (pilotos.length > 0) {%>
        <% for (let i=0; i<pilotos.length; i++) { %>
            <div class="card">
                <img src="/<%=pilotos[i].foto %>" alt="Foto del Piloto">
                <div class="card-content">
                    <h3>Nombre: <span><%=pilotos[i].nombre %></span></h3>
                    <p>Nacionalidad: <span><%= pilotos[i].nacionalidad %></span></p>
                    <p>Escudería Actual: <span><%= pilotos[i].escuderia %></span></p>
                    <p>Victorias: <span><%= pilotos[i].victorias %></span></p>
                    <div class="buttons">
                        <a href="/editar/<%=pilotos[i].id%>" class="edit">Editar</a>
                        <a href="/eliminar/<%=pilotos[i].id%>" class="delete">Eliminar</a>
                    </div>
                </div>
            </div>
            <% } %>
    <% } else { %>
        <h1> No hay pilotos insertados </h1>
    <% } %>
    <a href="/agregar" class="add-button">Agregar Piloto</a>
</body>
</html>
